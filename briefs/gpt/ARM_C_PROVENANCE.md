# Arm C provenance

These three Markdown files are the GPT conversational generation artifacts for cross-model validation. They were generated from the supplied `genai_input_facts.json` player facts. No validator result is asserted here; the project's frozen `genai_fidelity_check.py` must be run externally against these exact files.
